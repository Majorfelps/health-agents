# Deps extras só para rodar a suíte de testes (api/tests/).
# Instalar em cima de requirements.txt: pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
httpx==0.28.1
testcontainers[postgres]==4.9.0
